import requests

# Core style rules for the assistant
SYSTEM_RULES = (
    "You are a senior travel advisor."
    "\n- Talk like a human—warm, concise, confident."
    "\n- If the user greets (e.g., 'hi/hello'), reply in ONE short line and ask ONE relevant question. Do NOT dump tips or pick a destination."
    "\n- Answer in 2–4 crisp sentences unless the user asks for a list/itinerary."
    "\n- Include place-true anchors (areas, seasons, typical timing/prices) when relevant."
    "\n- If you used web/context, NEVER mention sources or URLs."
    "\n- No filler, no generic marketing fluff."
)

def generate_answer(query: str, context: str | None = None) -> str:
    """
    Uses local/remote model API to generate an answer.
    Falls back to a stub if no API is wired in yet.
    """
    prompt = SYSTEM_RULES + "\n\nUser: " + query
    if context:
        prompt += "\n\nContext:\n" + context
    #Replace with your model call; stubbed here
    return f"(stub) Answer for: {query}"
